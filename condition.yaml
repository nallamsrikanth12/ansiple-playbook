- name: conditions
  hosts: web
  become: yes
  tasks:
  - name: check the user exist or not
    ansible.builtin.command: id expense
    ignore_errors: true

  - name: say hello
    ansible.builtin.debug:
      msg: "say hello"